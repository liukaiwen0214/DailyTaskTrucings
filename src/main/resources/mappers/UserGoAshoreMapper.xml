<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imax.mapper.UserGoAshoreMapper">

    <select id="UserLoginMapper" resultType="java.lang.Integer">
        select count(*) from DailyTaskTrucingSystem.UserTable where uid = #{uid} and password = #{pwd}
    </select>
</mapper>